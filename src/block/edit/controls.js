import {
	BlockControls,
	InspectorControls,
} from '@wordpress/block-editor';

const controls = () => (
	<>
		<BlockControls>
		</BlockControls>
		<InspectorControls>
		</InspectorControls>
	</>
);

export default controls;
